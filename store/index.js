/* const cantidadProductos = Number(prompt("Ingresa el número de productos que deseas comprar"))
let totalApagar = 0
let resumenCompra =""
for (let index = 1; 
    index <= cantidadProductos; 
    index++) {
    const nombre = prompt("¿Qué producto desea llevar?");
    const cantidad = Number(prompt("¿Cuántas unidades?"));
    const precio = Number(prompt("¿Cuánto sale cada unidad?"));
    const subtotal = cantidad * precio;
    totalApagar = totalApagar + subtotal;
    const mensajeCompra = "Has incluido en el carrito " + cantidad + " unidades de " + nombre +" a "+ precio +" soles " + " por unidad. "
    resumenCompra += mensajeCompra;
    alert(resumenCompra);
    }
    alert("Este es el resumen de tu compra "+
    resumenCompra+ "El total a pagar por la compra es: " +totalApagar); */

/* const comprar = () => {
  let totalApagar = 0;
  for (let index = 1; index <= 2; index++) {
    const nombre = prompt("¿Qué producto desea llevar?");
    const cantidad = Number(prompt("¿Cuántas unidades?"));
    const precio = Number(prompt("¿Cuánto sale cada unidad"));
    const subtotal = cantidad * precio;
    totalApagar = totalApagar + subtotal;
  }
  console.log(totalApagar);
  return totalApagar;
}; */

/* const total1 = comprar();
const total2 = comprar();
const total3 = comprar();
const total = total1 + total2 + total3; */
/* const total = comprar() + comprar() + comprar();
console.log("Las ventas totales fueron "+total)
 */